{
  "mcpServers": {
    // Add MCP servers here. Each key is the server name Claude will use.
    // Run `claude mcp add` in your terminal to add servers interactively,
    // or fill in manually. Docs: https://docs.anthropic.com/en/docs/claude-code/mcp
    //
    // Example — filesystem access (built-in):
    // "filesystem": {
    //   "command": "npx",
    //   "args": ["-y", "@modelcontextprotocol/server-filesystem", "/Users/you/dev"]
    // },
    //
    // Example — GitHub (read PRs, issues, repos):
    // "github": {
    //   "command": "npx",
    //   "args": ["-y", "@modelcontextprotocol/server-github"],
    //   "env": { "GITHUB_TOKEN": "your-token-here" }
    // },
    //
    // Example — AWS (query resources, logs, etc.):
    // "aws": {
    //   "command": "npx",
    //   "args": ["-y", "mcp-server-aws"]
    // }
  }
}
