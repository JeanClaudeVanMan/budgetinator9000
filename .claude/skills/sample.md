---
name: sample
description: Sample skill — replace this with a real workflow. Skills are invoked with /sample or triggered automatically when their description matches the task.
---

# Sample Skill

<!-- This is a placeholder. Replace with a real workflow prompt.

A skill is just a markdown file with instructions for Claude to follow.
The `description` in frontmatter is what Claude reads to decide when to use it automatically.

Ideas for skills in this project:
  - add-lambda: scaffold a new lambda directory + CDK construct
  - deploy: run cdk diff then confirm before cdk deploy
  - review-lambda: check a lambda handler follows project conventions

Docs: https://docs.anthropic.com/en/docs/claude-code/skills
-->
